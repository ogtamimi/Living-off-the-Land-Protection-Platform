<Window x:Class="OGT.WatchTower.App.Views.SystemReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Security Report" Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#1E1E1E" CornerRadius="12" BorderBrush="#333333" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="System Security Report" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Background="Transparent" Foreground="#888888" BorderThickness="0" FontSize="16"
                        Click="Close_Click" Cursor="Hand"/>
            </Grid>

            <!-- Status Card -->
            <Border Grid.Row="1" x:Name="StatusBorder" Background="#2C2C2C" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="SYSTEM STATUS" Foreground="#888888" FontSize="12" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Ellipse x:Name="StatusDot" Width="12" Height="12" Fill="#00FF88" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock x:Name="StatusText" Text="SECURE" Foreground="White" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Threat Details -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <TextBlock Text="LATEST THREAT ANALYSIS" Foreground="#888888" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <Border Background="#252525" CornerRadius="8" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Threat Name" Foreground="#888888" FontSize="11"/>
                            <TextBlock x:Name="ThreatNameText" Text="None" Foreground="White" FontSize="14" Margin="0,2,0,10" TextWrapping="Wrap"/>

                            <TextBlock Text="Severity" Foreground="#888888" FontSize="11"/>
                            <Border x:Name="SeverityBadge" Background="#333333" HorizontalAlignment="Left" CornerRadius="4" Padding="8,2" Margin="0,2,0,10">
                                <TextBlock x:Name="SeverityText" Text="-" Foreground="White" FontSize="12"/>
                            </Border>

                            <TextBlock Text="Location / Process" Foreground="#888888" FontSize="11"/>
                            <TextBlock x:Name="LocationText" Text="-" Foreground="#AAAAAA" FontSize="13" Margin="0,2,0,10" TextWrapping="Wrap"/>

                            <TextBlock Text="Potential Impact" Foreground="#888888" FontSize="11"/>
                            <TextBlock x:Name="DescriptionText" Text="No threats detected in the current session." Foreground="#AAAAAA" FontSize="13" Margin="0,2,0,10" TextWrapping="Wrap"/>

                            <TextBlock Text="Action Taken" Foreground="#888888" FontSize="11"/>
                            <TextBlock x:Name="ActionText" Text="-" Foreground="White" FontSize="13" FontWeight="SemiBold" Margin="0,2,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Button Grid.Row="3" Content="Close Report" Background="#333333" Foreground="White" 
                    Height="40" Margin="0,20,0,0" Style="{StaticResource ActionButton}"
                    Click="Close_Click"/>
        </Grid>
    </Border>
</Window>