<UserControl x:Class="OGT.WatchTower.App.Views.IntelligenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="0,0,20,0">
            <TextBlock Text="Threat Intelligence" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
            
            <!-- File Hash Check -->
            <Border Background="#252525" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Check File (VirusTotal)" FontSize="16" Foreground="#00D4FF" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtFilePath" ToolTip="Enter full file path..." Background="#333333" Foreground="White" BorderThickness="0" Padding="5" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Check" Width="80" Style="{StaticResource ActionButton}" Click="CheckFile_Click"/>
                    </Grid>
                    <TextBlock x:Name="LblFileResult" Text="Result: Waiting..." Foreground="#AAAAAA" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- IP Check -->
            <Border Background="#252525" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Text="Check IP (AbuseIPDB)" FontSize="16" Foreground="#00D4FF" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtIpAddress" ToolTip="Enter IP address..." Background="#333333" Foreground="White" BorderThickness="0" Padding="5" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Check" Width="80" Style="{StaticResource ActionButton}" Click="CheckIp_Click"/>
                    </Grid>
                    <TextBlock x:Name="LblIpResult" Text="Result: Waiting..." Foreground="#AAAAAA" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
