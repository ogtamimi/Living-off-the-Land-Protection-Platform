<UserControl x:Class="OGT.WatchTower.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="0,0,20,0">
            <!-- Protection -->
            <TextBlock Text="Protection Settings" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
            <Border Background="#252525" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <CheckBox x:Name="ChkAutoSuspend" Content="Auto Suspend Suspicious Processes" Foreground="White" Margin="0,5"/>
                    <CheckBox x:Name="ChkAutoKill" Content="Auto Terminate High Risk (Dangerous)" Foreground="White" Margin="0,5"/>
                    <CheckBox x:Name="ChkSysmon" Content="Enable Sysmon Monitoring" Foreground="White" Margin="0,5"/>
                </StackPanel>
            </Border>

            <!-- API Keys -->
            <TextBlock Text="Integrations (API Keys)" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
            <Border Background="#252525" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="VirusTotal API Key" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtVirusTotal" Background="#333333" Foreground="White" BorderThickness="0" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="AbuseIPDB API Key" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtAbuseIpDb" Background="#333333" Foreground="White" BorderThickness="0" Padding="5"/>
                </StackPanel>
            </Border>

            <!-- Save -->
            <Button Content="Save Settings" HorizontalAlignment="Left" Width="150" Height="40" Style="{StaticResource ActionButton}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
